FROM python:3.11-slim

WORKDIR /app

COPY creator_flow_agent/requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY creator_flow_agent /app/root_agent

ENV PORT=8080

CMD ["python", "-m", "google.adk.cli", "api_server", "--host", "0.0.0.0", "--port", "8080", "/app"]
